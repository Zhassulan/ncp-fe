image: docker:latest
services:
  - docker:dind
cache:
  key: cache-global-key
  paths:
    - dist/
    - node_modules/

stages:
  - build
  - package

# - deploy

angular-build:
  image: docker.tele2.kz/ncp/angular-cli
  stage: build
  before_script:
    - ssh-keyscan -H $SSH_SERVER_HOST >> ~/.ssh/known_hosts

  script:
    #- export SSHPASS=$USER_PASS
    #- sshpass -e scp ncp-frontend-app-deployment.yaml kube@$SSH_SERVER_HOST:/home/kube/ncp-frontend-app-deployment.yaml
    - npm install
    - ng build --configuration=production --base-href /ncp-frontend/ --output-hashing=all --prod=true
  artifacts:
    paths:
      - dist/*

docker-build:
  stage: package
  script:
    - docker build --cache-from $CONTAINER_IMAGE:latest -t docker.tele2.kz/ncp/ncp-frontend-app .
    - docker login -u gitlab-ci-token -p $CI_BUILD_TOKEN docker.tele2.kz
    - docker push docker.tele2.kz/ncp/ncp-frontend-app

#docker-deploy:
#  stage: deploy
#  image: governmentpaas/git-ssh
#  environment:
#    name: development
#  only:
#  - master
#  before_script:
#  - eval $(ssh-agent -s)
#  - echo "$SSH_PRIVATE_KEY" | tr -d '\r' | ssh-add - > /dev/null
#  - mkdir -p ~/.ssh
#  - chmod 700 ~/.ssh
#  - ssh-keyscan "$SSH_SERVER_HOST" >> ~/.ssh/known_hosts
#  - chmod 644 ~/.ssh/known_hosts
#  script:
#  - ssh -T root@$SSH_SERVER_HOST "
#    /usr/local/bin/docker-compose -f /data/docker-compose/$CI_PROJECT_NAME/docker-compose.yaml down
#    && /usr/bin/docker login -u gitlab-ci-token -p $CI_BUILD_TOKEN docker.tele2.kz
#    && /usr/local/bin/docker-compose -f /data/docker-compose/$CI_PROJECT_NAME/docker-compose.yaml pull
#    && /usr/local/bin/docker-compose -f /data/docker-compose/$CI_PROJECT_NAME/docker-compose.yaml up -d
#    && /usr/bin/docker logout docker.tele2.kz"
