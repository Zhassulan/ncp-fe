import { PublicRegistryInDto } from './public-registry-in-dto';

describe('PublicRegistryInDto', () => {
  it('should create an instance', () => {
    expect(new PublicRegistryInDto()).toBeTruthy();
  });
});
