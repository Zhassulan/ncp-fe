export class DialogData {

    itemName: string;
    itemMessage: string;

    constructor(itemName: string, itemMessage: string) {
        this.itemName = itemName;
        this.itemMessage = itemMessage;
    }
}
