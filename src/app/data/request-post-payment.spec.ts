import { RequestPostPayment } from './request-post-payment';

describe('RequestPostPayment', () => {
  it('should create an instance', () => {
    expect(new RequestPostPayment()).toBeTruthy();
  });
});
